#include<stdio.h> 
int main(){ 
  int a,b, multiply;
 printf ("enter the value of a"); 
  scanf("%d",&a);
  printf("enter the value of b");
  scanf("%d",&b);
multiply=a*b;
  printf("the multiplication of %d and%d is %d\n",a,b,multiply);
  return 0;
  }
