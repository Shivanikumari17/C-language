#include<stdio.h>
  int main() {
  int l, b, perimeter ;
  printf("Enter the Length of l "); scanf("%d", &l); 
  printf(" Enter the value of b");
    scanf("%d", &b);
    perimeter = 2*(l+b);
  printf(" perimeter of rectangle: %d", perimeter);

  return 0;
}
