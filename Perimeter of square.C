#include<stdio.h>
  int main() {
  int side, perimeter;
  printf("Enter the Length of Side: "); scanf("%d", &side);
  perimeter = 4 * side;
  printf("perimeter of Square: %d", perimeter);

  return (0);
}
